#include <iostream>
using namespace std;

int max_of_four(int a, int b, int c, int d) {
    int maks = a;
    if (b > maks) {
        maks = b;
    }
    if (c > maks) {
        maks = c;
    }
    if (d > maks) {
        maks = d;
    }
    return maks;
}
